# Dependencies
node_modules/
.pnp/
.pnp.*

# Build output
dist/
build/
out/
.next/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Tests / coverage
coverage/
.nyc_output/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code — machine-local permissions
.claude/settings.local.json

# BMAD — per-user config (team config in _bmad/config.toml stays tracked)
_bmad/config.user.toml

# Story 1.2 — a real device push endpoint, never committed
.push-subscription.json

# Story 2.4a — a signed session token for the outbox verification account. One was committed
# in 898b7e7 and removed a commit later; it stays in history (expired, throwaway account).
# This is what stops the next verification run from doing it again.
.ob-token

# Serwist build output
public/sw.js
public/sw.js.map

# Supabase CLI scratch — machine-local, rewritten on link and on version check
supabase/.temp/
