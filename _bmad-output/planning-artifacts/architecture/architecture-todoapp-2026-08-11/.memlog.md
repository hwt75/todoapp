---
scope: todoapp v1 - iOS doer app + referee web app
purpose: Fix the invariants that keep independently built units from diverging
altitude: initiative
updated: 2026-08-12T09:19
---

- (constraint) Trình độ user: iOS/Swift ở mức LÀM ĐƯỢC VIỆC — đã đẩy app lên store hoặc làm app có gọi API, lưu dữ liệu, xử lý quyền hệ thống. CHƯA thành thạo background tasks, CoreLocation, notification nâng cao. Kiến trúc phải đỡ user ở đúng ba chỗ đó vì đó lại là ba chỗ sản phẩm này sống chết
- (direction) Chế độ: Coaching. Sản phẩm phụ: user muốn THÊM bản thiết kế đầy đủ có lý do cho từng quyết định, ngoài spine
- (constraint) Nguồn đã chốt, kế thừa im lặng không hỏi lại: prd.md (28 FR, final), addendum.md, DESIGN.md, EXPERIENCE.md. Ràng buộc UX nặng nhất mang sang: cổng chặn buổi sáng phải chặn app mà không chặn máy; thông báo phải dai và tự đẩy lại; web trọng tài dùng email không dùng push
- (version) Kiểm chứng qua web 2026-08: iOS 26 / Xcode 26 / SwiftUI 7 là hiện hành (iOS 26 phát hành 15-09-2025, Apple đổi sang đánh số theo năm). Supabase Swift SDK là SDK chính chủ, 5 năm phát triển, 132 bản phát hành, dùng Swift concurrency hiện đại, cài qua SPM, có Auth/Realtime/Storage/PostgREST/Functions tách gói được
- (version) PHÁT HIỆN QUAN TRỌNG cho cổng chặn buổi sáng: AlarmKit (mới từ iOS 26) cho phép báo thức xuyên qua chế độ im lặng và Focus — đúng thứ EXPERIENCE.md cần. Critical Alerts thì KHÔNG dùng được: Apple chỉ cấp cho y tế và an toàn, app cam kết cá nhân chắc chắn bị từ chối. Time-Sensitive cần entitlement riêng và yếu hơn
- (constraint) Giới hạn geofence iOS đã kiểm chứng: tối đa 20 vùng cho mỗi app, bán kính khuyến nghị 100m, callback trễ tới 20 giây, app chỉ được đánh thức khoảng 10 giây để xử lý. CoreLocation KHÔNG có sẵn tính năng đo thời gian lưu lại — phải tự ghép mốc vào/ra rồi tự tính. Từ iOS 18 phải khởi tạo và giữ CLServiceSession thì mới nhận được sự kiện nền
- (constraint) RÀNG BUỘC LỚN từ user: KHÔNG có MacBook, KHÔNG có tài khoản Apple Developer, chỉ có iPhone + iPad (và PC Windows 11). Làm đổ giả định app iOS gốc nằm dưới cả PRD lẫn UX
- (version) Kiểm chứng 2026-08: KHÔNG cần MacBook — EAS Build/Codemagic build trên Mac đám mây. NHƯNG tài khoản Apple Developer 99usd/năm là bắt buộc để ký code và cài lên máy thật, không có đường vòng chính thống (Xcode + Personal Team miễn phí vẫn đòi máy Mac)
- (version) PHÁT HIỆN QUYẾT ĐỊNH: sideload bằng AltStore + AltServer trên Windows 11 là khả thi (ký bằng Apple ID miễn phí, chứng chỉ 7 ngày, tự gia hạn qua WiFi). NHƯNG Personal Team miễn phí KHÔNG ký được quyền Push Notifications và iCloud — tài liệu Apple ghi rõ, phải trả phí mới có aps-environment. App sideload được nhưng không nhắc được gì, tức là vô dụng với sản phẩm chạy hoàn toàn bằng thông báo. ĐƯỜNG SIDELOAD ĐÓNG
- (version) PWA trên iOS 16.4+: push CÓ chạy nhưng chỉ khi cài ra màn hình chính; iOS 26 mặc định mở site đã thêm vào màn hình chính như app riêng. KHÔNG có Background Sync API, không chạy nền được, nên geofence nền và cảm biến chuyển động nền là bất khả. Hạn chế EU (DMA) không áp dụng cho Việt Nam
- (decision) NỀN TẢNG = PWA, không phải iOS gốc. Cài ra màn hình chính iPhone, có push. Đây là ghi đè lớn nhất từ đầu dự án: PRD (§5, §7.1, toàn bộ ngữ cảnh iOS) và EXPERIENCE.md (mục Foundation ghi 'iOS, native') đều phải sửa lại. Nguyên nhân gốc: không có tài khoản Apple Developer, và Apple ID miễn phí không ký được quyền push
- (decision) HỆ QUẢ: hai Auto-check dùng cảm biến nền (vị trí+thời gian lưu lại, chuyển động điện thoại) RỚT khỏi v1 vì web không chạy nền được trên iOS. Hai cái còn sống là tài khoản ngoài và đồng hồ bấm giờ — cả hai đều tính ở máy chủ nên không cần cảm biến. Cổng chặn buổi sáng mất AlarmKit, hạ xuống thành thông báo đẩy tự đẩy lại
- (decision) HỆ QUẢ TỐT: app của user và web của trọng tài gộp thành MỘT codebase, khác nhau ở vai trò. Cắt mạnh rủi ro số 2 của PRD (phạm vi quá lớn cho một người build)
- (decision) [ADOPTED] Máy chủ là nơi phán xử, điện thoại chỉ báo cáo sự kiện. User chọn phương án B. Lý do quyết định: FR-9 đòi đồng hồ hết hạn 48 giờ chạy trong lúc user im lặng — điện thoại không cung cấp được đồng hồ đó. Thêm nữa trọng tài phải nhìn thấy cùng một sự thật ngay cả khi user chưa mở app
- (constraint) Trình độ web của user: THÀNH THẠO (React/TypeScript, hiểu service worker). Không cần chọn bộ công cụ dễ học, không cần spine đỡ tay
- (version) Hạt giống công nghệ đã kiểm chứng hiện hành 2026-08: Next.js 16 (App Router, TypeScript), Serwist (bộ service worker đang được duy trì, kế thừa next-pwa), Supabase (Postgres + Auth + Storage + Edge Functions + pg_cron có sẵn cả trên gói miễn phí), web-push chuẩn VAPID, deploy Vercel. Lưu ý: Supabase gói miễn phí tạm dừng project khi không hoạt động — cron chạy hằng ngày nên không dính, nhưng phải biết
- (decision) MÔ HÌNH LÕI = sổ ghi sự kiện chỉ-thêm + phiên chốt định kỳ. Client chỉ gửi QUAN SÁT thô (mốc bắt đầu/kết thúc phiên, lời khai, sự kiện vào/ra vùng), CẤM gửi kết luận. Job định kỳ đọc sự kiện rồi ghi ra phán quyết. Lý do quyết định: có tiền thật và có người thứ hai đi thu, nên mỗi khoản nợ phải truy ngược được tới sự kiện sinh ra nó — không phải để chống gian lận mà vì một khoản nợ không giải thích được sẽ làm user mất niềm tin và gỡ app. Sổ nợ không phải màn hình, nó là mô hình dữ liệu
- (decision) Loại event sourcing đầy đủ: hai người dùng và năm task thì dựng lại trạng thái từ sự kiện mỗi lần đọc là quá tay. Giữ bảng phán quyết đã chốt làm projection
- (decision) Logic phán xử nằm trong HÀM POSTGRES, pg_cron gọi thẳng, toàn bộ phiên chốt trong MỘT giao dịch. User chọn. Lý do: job chết giữa chừng thì cuộn lại sạch, không bao giờ có chuyện cộng tiền rồi mới chết; không có chặng HTTP nào để hỏng
- (decision) ĐIỀU CHỈNH bắt buộc cho lựa chọn trên: Postgres không ký được VAPID, không nên phân tích JSON API ngoài, không gửi email. Dùng HỘP THƯ ĐI GIAO DỊCH — hàm chốt ghi phán quyết VÀ việc-cần-gửi trong cùng một giao dịch; worker riêng rút hộp thư ra thực thi. Giữ nguyên thứ user thật sự chọn (phán xử nguyên tử) mà vẫn gọi được ra thế giới bên ngoài
- (event) Cổng rà soát: lint_spine sạch 0 phát hiện. Ba lens ngữ nghĩa chạy tuần tự (kiểm chứng công nghệ, đối kháng, rubric) — kết quả ở reviews/. Tổng 1 nghiêm trọng, 6 cao, 3 vừa, 3 nhẹ
- (decision) AD-8 MỞ RỘNG sau lens đối kháng: không chỉ tiền mà MỌI giá trị dẫn xuất (chain, tiến độ định mức, số dư sổ nợ, mọi projection) đều chỉ có đúng một người ghi là hàm chốt phiên. Lỗ gốc: FR-17 nói ngày ân hạn giữ chuỗi, nên đường ân hạn và settle_day có thể cùng ghi chain và ra kết quả khác nhau, cả hai đều đúng luật
- (decision) Thêm AD-13: Auto-check phải có kết quả cuối trước khi chốt ngày đó; settle_day từ chối chạy nếu chưa thử kiểm. 'Unavailable' nghĩa là ĐÃ THỬ VÀ HỎNG, không phải CHƯA THỬ. Lỗ gốc: xếp lịch settle 00:05 và drain check 00:10 thì check ngoài luôn unavailable vĩnh viễn, biến task đã gắn Auto-check thành task phải khai tay mỗi sáng
- (decision) Thêm AD-14: sự kiện thuộc về ngày chứa mốc BẮT ĐẦU, thời lượng dồn hết vào ngày đó, không bao giờ cắt đôi qua nửa đêm. Lỗ gốc: phiên tập trung 23:50-00:20 có ba cách quy ngày đều hợp lệ, mà phạt phẳng thì lệch một phiên là khác nhau giữa ngày hỏng và ngày sạch
- (decision) Thêm AD-15: khoản phạt chuyển khỏi trạng thái treo đúng MỘT lần, bằng cập nhật có điều kiện gác theo trạng thái hiện tại; người ghi đầu tiên thắng, người sau là no-op. Lỗ gốc: settle_week bỏ phạt do hết hạn và appeal.ruled chuyển thành nợ có thể xảy ra cùng lúc, hai kết cục ngược nhau, 500k quyết định bởi giao dịch nào commit trước
- (decision) Thêm AD-16: hàm chốt phiên từ chối chạy ngoài lịch trừ khi truyền cờ ghi đè, và cờ đó bị từ chối với tài khoản thật. Lỗ gốc: chỉ có một project Supabase, vừa code vừa chạy thật thì ghi nhầm một khoản phạt vào sổ chỉ-thêm không xoá được
- (decision) AD-3 bổ sung: worker rút hộp thư được đánh thức bằng lịch pg_cron RIÊNG qua pg_net, độc lập với mọi phiên chốt — hộp thư phải rút cả khi không có phiên chốt nào chạy. Thêm pg_net vào bảng công nghệ (trước đó thiếu, nghĩa là hộp thư đầy mà không ai rút)
- (decision) Quan trắc: TỔNG KẾT NGÀY CHÍNH LÀ NHỊP TIM. Settlement chết (migration hỏng, hết quota, project gói miễn phí bị tạm dừng do không hoạt động) thì tổng kết ngừng tới — đó là tín hiệu. Đáng lo vì mọi kiểu chết âm thầm đều có lợi cho user (không ngày nào đóng, không tiền nào cộng), nên user không có lý do tự nhiên nào để phát hiện cơ chế đã chết
- (event) Vá ngược nguồn sau bước ngoặt PWA. prd.md: §5 đổi 'hai bề mặt iOS+web' thành 'một PWA hai vai trò'; §4.1 bảng đánh dấu hai Auto-check hoãn kèm giải thích vì sao không mất sức nặng; FR-6 và FR-7 gắn [DEFERRED]; §7.1 còn hai Auto-check; §7.2 thêm mục hoãn client native với lý do và điều kiện quay lại; §6 gỡ 'không Android' vì PWA chạy được; UJ-3 ghi chú v1 chạy trên TryHackMe thay vì gym; câu hỏi mở số 4 (trình độ Swift) đánh dấu đã giải quyết và không còn liên quan; câu hỏi số 2 (TryHackMe) nâng thành ẩn số giá trị nhất vì giờ là Auto-check duy nhất chạm ra ngoài
- (event) EXPERIENCE.md: mục Foundation viết lại cho PWA, nói rõ mất gì (chạy nền, cổng chặn cấp báo thức) và KHÔNG mất gì (tiền, trọng tài, luật chốt); Cài đặt thay quyền vị trí/chuyển động bằng trạng thái cài ra màn hình chính — không cài thì không có push, không push thì không có sản phẩm; KF-3 ghi chú chạy trên TryHackMe ở v1
- (assumption) GIẢ ĐỊNH MỚI, RỦI RO CAO NHẤT, đã thêm vào mục giả định của PRD: Web Push của PWA cài màn hình chính đủ tin cậy để gánh một sản phẩm mà mọi năng lực cốt lõi đều là thông báo. Đã kiểm chứng là iOS 16.4+ hỗ trợ, NHƯNG chưa kiểm chứng trên tay user. Phải thử cái này trước khi build bất cứ thứ gì khác
- (event) Viết SOLUTION-DESIGN.md theo yêu cầu của user: lý do đằng sau từng quyết định, các phương án đã cân nhắc và loại, câu chuyện bước ngoặt nền tảng, bốn va chạm mà lens đối kháng tìm ra, và thứ tự nên build
- (event) spine finalized
