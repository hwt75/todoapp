---
topic: todoapp UX
updated: 2026-08-11T14:05
---

- (decision) Ngôn ngữ giao diện: TIẾNG ANH. Toàn bộ microcopy trong DESIGN.md/EXPERIENCE.md viết bằng tiếng Anh
- (assumption) LO NGẠI đã nêu, user vẫn chọn tiếng Anh: user và trọng tài đều là người Việt; câu hỏi buổi sáng, can thiệp ngày thứ hai và tin nhắn đòi tiền là những chuỗi ăn vào cảm xúc, sang ngôn ngữ thứ hai sẽ nhẹ lực. Có phương án giữa chưa chào: chrome tiếng Anh, riêng 3-4 chuỗi nặng cảm xúc để tiếng Việt
- (decision) Chế độ làm việc UX: Coaching — dùng công cụ dựng hình (bảng màu, hướng thiết kế, khung màn hình) để user nhìn rồi chọn
- (event) Nhận định mở đầu: mặt chính của sản phẩm là THÔNG BÁO TRÊN MÀN HÌNH KHOÁ, không phải màn hình trong app. Câu chữ quan trọng hơn bảng màu — phần Voice and Tone phải đào sâu hơn phần màu sắc
- (override) MÂU THUẪN VỚI PRD ĐÃ CHỐT: PRD §7.2 và FR-1 loại 'in-app Commitment editing' khỏi v1 (5 task nạp cứng như cấu hình). User trong khâu UX yêu cầu có màn hình thiết lập task. Ghi đè: v1 CÓ giao diện tạo/sửa task. Hệ quả: PRD cần cập nhật FR-1 và §7.2 khi xong UX
- (decision) User yêu cầu app iOS có giao diện thật, không chỉ sống bằng thông báo: tối thiểu là màn thiết lập task và màn xem streak. Dấu '...' cho thấy còn màn khác chưa liệt kê — phải chốt danh sách bề mặt trước khi đóng IA
- (event) User không trả lời ba câu về gu thẩm mỹ mà trả lời bằng nhu cầu chức năng. Kiểu tư duy thiên về tính năng — đổi chiến thuật: chốt danh sách màn hình trước, phần nhìn để sau và cho user chọn từ bản dựng sẵn thay vì hỏi trừu tượng
- (decision) Màn chủ = HÔM NAY, và chuỗi nằm ngay trên đó chứ không phải màn riêng. Màn 'Chuỗi & tiến độ' hạ xuống thành màn chi tiết xem sâu, không phải điểm vào
- (decision) Danh sách bề mặt ĐÓNG ở 12 màn đã liệt kê. User xác nhận không còn màn nào khác ('không còn')
- (decision) TIỀN NỢ HIỆN RÕ, và là tổng cộng dồn TỪ LÚC BẮT ĐẦU chứ không phải nợ của riêng kỳ hiện tại. User chọn dù đã được cảnh báo rằng con số to dần trước mắt chính là thứ dễ khiến gỡ app nhất khi không có trần phạt. Quyết định của user, đã ghi
- (change) Hệ quả cho màn Hôm nay: nó phải gánh cùng lúc ba thứ vốn đá nhau về giọng — 5 việc hôm nay, chuỗi (thành tích), và tổng nợ cộng dồn (thất bại). Bố cục phải giải được chuyện đặt một con số thất bại đang lớn dần cạnh một con số thành tích mà không biến màn chính thành bảng cáo trạng
- (decision) Bố cục màn Hôm nay = PHƯƠNG ÁN B: tổng nợ cộng dồn là khối lớn nhất, nằm trên cùng, trên cả danh sách việc. User chọn sau khi đã được nêu rủi ro hai lần. Mock lưu tại .working/today-screen-2026-08-11.html
- (decision) HỆ MÀU NGỮ NGHĨA, mỗi màu đúng một nghĩa: xanh lá = đã xong + chuỗi còn sống (con số trong viên là số ngày liên tiếp); cam = định mức tuần đang gấp, chưa hỏng (hiện dạng '1/3 · 3 days', đúng phép tính FR-4); đỏ = đã trượt và tiền nợ, không dùng cho gì khác; xám nhạt = chưa tới lượt, cố ý không tô màu vì chưa làm không phải là lỗi
- (decision) LUẬT BẤT BIẾN cho màn Hôm nay: không bao giờ đỏ toàn tập. Ngày trượt cả 5 việc thì các dòng vẫn xám, chỉ ô nợ đỏ. Lý do: PRD xác định 'mở app ra thấy một bảng đỏ' chính là thứ làm user ngừng mở app
- (override) ĐỔI MÔ HÌNH XÁC MINH so với PRD: PRD coi Verification Tier là thuộc tính cố định của từng Commitment (Machine/Declared/Timer) và gán cứng cho 5 task. User yêu cầu: các yếu tố kiểm tra tự động (GPS, cảm biến...) thành TUỲ CHỌN lúc tạo task; chọn thì mới dùng để kiểm. Hệ quả: mọi task mặc định là tự khai, kiểm tự động là lớp phụ trợ gắn thêm — không còn là ba tầng tách rời. PRD FR-1, FR-2, FR-6..8 và §4.3 phải viết lại
- (change) Mô hình mới gọn hơn và ăn khớp với yêu cầu có màn thiết lập task: task nào cũng khai báo được bằng tay; bật kiểm tự động thì máy tự điền hộ lời khai. Máy xác nhận được thì task tự tick, user không bị hỏi. Máy báo trượt thì mở đường khiếu nại. Không bật gì thì chỉ còn tự khai
- (event) TREO LẠI, chưa chốt: chọn giọng cho câu hỏi buổi sáng (1 Ledger / 2 Coach / 3 Stakes). Đã dựng ba bản cho user xem, user chuyển sang việc khác trước khi chọn
- (decision) GIẢI QUYẾT XUNG ĐỘT MÁY vs NGƯỜI = phương án (c), phụ thuộc vào tiền: task CÓ gắn 500k thì phán quyết của máy thắng, muốn lật phải khiếu nại và để trọng tài phân xử; task KHÔNG gắn tiền thì lời khai của user thắng, máy chỉ là tiện ích tick hộ, không có khiếu nại. Bám nguyên tắc chạy suốt từ brief: chỉ làm chặt ở chỗ có tiền
- (change) Hệ quả cho màn thiết lập task: công tắc tiền và công tắc kiểm tự động không độc lập về mặt ý nghĩa. Bật cả hai thì màn hình phải nói rõ tại chỗ rằng máy sẽ có tiếng nói cuối cùng và đường lật là khiếu nại — nếu không user sẽ chỉ hiểu ra điều đó vào đúng hôm bị trừ oan 500k
- (decision) GIỌNG = phương án 2 'Coach': nói với user ở ngôi thứ hai, ngắn, không doạ, không nêu tiền lúc đang hỏi. Mẫu gốc: 'Morning. Day 5 is waiting.' / 'Did last night hold?'. Lý do loại giọng 3: nêu tiền ngay trước khi hỏi làm câu trả lời thật trở thành câu trả lời đắt, mà nói dối thì miễn phí và không ai kiểm được — mua lấy lời nói dối là hỏng cả cơ chế
- (decision) Menu kiểm tự động CHỐT ở 4 mục: vị trí + thời gian ở lại, chuyển động điện thoại, đồng hồ bấm giờ, tài khoản ngoài. ẢNH CHỤP không nằm trong menu — ảnh cũ dùng lại được nên không kiểm được gì; ảnh chỉ xuất hiện ở màn khiếu nại làm bằng chứng gửi trọng tài
- (decision) Chữ trên giao diện dùng tiếng người, không dùng thuật ngữ đặc tả: 'Do it / Avoid it / Put hours in' thay cho Do/Abstain/Open-ended. Glossary là từ vựng nội bộ cho tài liệu và code, không leo lên màn hình
- (decision) Chọn kiểu 'Avoid it' thì toàn bộ 4 cách kiểm tự động bị vô hiệu và xám đi, kèm câu nói thẳng rằng không gì kiểm được việc này và lời khai buổi sáng chính là hồ sơ. Nguyên tắc trung thực của sản phẩm phải hiện ra ngay lúc tạo task, không phải lúc mất tiền
- (decision) Công tắc tiền mặc định TẮT khi tạo task mới. Bật tiền phải là hành động có chủ ý
- (decision) Phiên tập trung: số to là thời gian phiên hiện tại nhưng số mang nghĩa là 'banked today X/3:00' kèm thanh tiến độ xanh, vì cam kết là định mức ngày chứ không phải một phiên. Nút ghi 'Stop and bank it' — dừng là gửi vào kho, không phải mất. Có câu nói thẳng 'Nothing here watches what you're doing' để user không ngại bấm start
- (decision) Màn khiếu nại phải nói CHÍNH XÁC máy thấy gì và thiếu bao nhiêu ('Location saw you for 4 minutes. It needed 30'), cấm dùng câu chung chung kiểu 'xác minh thất bại'. Con số cụ thể là thứ duy nhất làm user tin đây là lỗi kỹ thuật chứ không phải app tuỳ tiện. Dùng màu vàng chứ không đỏ vì tiền đang treo chưa mất
- (decision) Quy tắc hết hạn xử thắng cho user (PRD FR-15) phải hiện trên CẢ HAI bề mặt: trên app của user ('if he doesn't get to it, it's dropped') và trên web của trọng tài ('ignore this and it's dropped in his favor on Sunday'). Nói ở phía trọng tài còn gỡ cảm giác tội lỗi cho nó — nó không phải nút thắt cổ chai
- (decision) Trạng thái RỖNG là trạng thái mặc định của web trọng tài và được thiết kế trước tiên: 'Nothing for you this week' kèm một mẩu thưởng nhỏ về tiến độ của user. Đây là đòn bẩy giữ chân duy nhất cho người bạn — rủi ro số 1 của dự án
- (decision) Web trọng tài KHÔNG phụ thuộc push (web push yếu, nhất là iOS Safari). Kênh chủ động sang trọng tài là EMAIL: 'You'll get an email if that changes'
- (decision) Nút của trọng tài dùng tiếng người: 'He did it' / 'He didn't' thay cho Approve/Reject
- (decision) HỆ NÚT: xanh lá = hành động đẩy tới, mỗi màn đúng một cái; đỏ = chỉ dành cho thao tác phá huỷ (xoá task), rất hiếm; chữ trơn không viền = rời đi mà không làm gì
- (decision) NGOẠI LỆ QUAN TRỌNG: hai nút khai báo buổi sáng 'It held' / 'I slipped' phải XÁM và giống hệt nhau. Tô xanh cho câu trả lời tốt và đỏ cho câu trả lời xấu là đánh thuế lên sự thật — không ai kiểm được user bấm gì, nên làm nút thật khó bấm hơn nút dối là mua lấy lời nói dối. Cùng nguyên tắc đã dùng khi loại giọng 3
- (decision) Nút phán xử của trọng tài ĐƯỢC tô màu (He did it xanh / He didn't đỏ) vì nó đang phán xử người khác, không phải tự thú — màu chỉ giúp bấm nhanh, không tạo áp lực xấu
- (decision) ĐỘ ĐẬM NÚT = MỨC 2 (xanh #97C459 chữ #173404, đỏ #F09595 chữ #501313), không dùng mức solid cũng không dùng mức nhạt. Lý do loại mức nhạt: tông nhạt đã được dùng cho NHÃN TRẠNG THÁI (viên chuỗi xanh nhạt, khối nợ đỏ nhạt, ô treo vàng nhạt); nút mà cùng tông thì không phân biệt được cái nào bấm được nếu không đọc chữ. Nếu sau này thấy chói thì hạ nhãn xuống nhạt hơn chứ không hạ nút
- (event) Đã viết DESIGN.md và EXPERIENCE.md từ 34 mục memlog. 6 màn không dựng hình được đặc tả bằng bảng trong EXPERIENCE.md mục Information Architecture, kèm luật riêng cho từng màn
- (event) Mock màn Hôm nay chuyển từ .working sang mockups/today-screen.html. Các mock khác (tạo task, phiên tập trung, khiếu nại, web trọng tài, sổ nợ, tổng kết, hệ nút) dựng trong hội thoại chứ chưa lưu thành tệp — quyết định từ chúng đã nằm trong hai đặc tả
- (event) Chạy 3 lens rà soát (bao phủ, mâu thuẫn nội tại, tiếp cận). Kết quả: 1 nghiêm trọng, 4 cao, 4 vừa, 2 nhẹ. Ghi ở review-coverage.md, review-contradiction.md, review-accessibility.md
- (change) Đã vá 9 phát hiện. EXPERIENCE.md: bỏ geofence khỏi Cài đặt (thuộc về từng task, không phải cài đặt chung); thêm trạng thái thứ năm cho web trọng tài kèm chuỗi chữ cho FR-18; thêm mẫu liên kết tài khoản ngoài và mẫu nút ngày ân hạn; cổng chặn buổi sáng viết lại thành chặn-app-không-chặn-máy kèm ràng buộc VoiceOver; thêm ngưỡng tương phản WCAG AA, luật Reduce Motion, và thứ tự đọc VoiceOver trên màn Hôm nay đảo so với thứ tự nhìn
- (change) DESIGN.md: luật một-nút-hành-động thu về phạm vi THẺ chứ không phải màn (web trọng tài hiện nhiều thẻ cùng lúc); luật không-toàn-đỏ mở rộng sang màn Sổ nợ với cách xử lý cụ thể — dòng Owed giữ nhãn nhưng không tô nền, chỉ Waived và Collected được tô, để màu trong sổ thuộc về những ngày đã ngã ngũ chứ không phải những ngày chưa
- (change) prd.md: FR-17 thêm ràng buộc ngày ân hạn phải tiêu được từ tổng kết ngày và từ dòng Sổ nợ, cấm chỉ cho tiêu qua can thiệp im lặng (nếu không thì phải im lặng 2 ngày mới dùng được quyền của mình); thêm FR-8b về Auto-check không chạy được — mất quyền hệ thống là KHÔNG KHẢ DỤNG chứ không phải TRƯỢT, và khi đó FR-2a không áp dụng vì không có kết quả máy nào để mà thắng; FR-18 nói rõ kênh email và trạng thái trên web trọng tài
- (decision) GỠ LỖ HỔNG NGHIÊM TRỌNG: khai báo không trả lời sẽ HẾT HẠN SAU 48 GIỜ VÀ TÍNH LÀ TRƯỢT. Lý do: nếu im lặng để ngày treo vĩnh viễn thì cách rẻ nhất để đối phó một lần trượt là ngừng trả lời — đúng hành vi sản phẩm sinh ra để chặn. Làm rõ luôn: nguyên tắc 'hết hạn xử thắng cho user' của FR-15 bảo vệ user khỏi sự chậm trễ của NGƯỜI KHÁC, không phải khỏi im lặng của chính mình; hai chuyện khác loại
- (change) Hệ quả tốt ngoài dự tính: câu can thiệp ngày thứ hai giờ đến TRƯỚC lần hết hạn đầu tiên, nên nó thành lời cảnh báo cuối chứ không phải lời chia buồn — hành động theo nó vẫn cứu được ngày. Gỡ luôn mâu thuẫn ô cửa buổi sáng: can thiệp được phép chiếm ô cửa vì nó không nuốt mất ngày nào. Thêm chuỗi chữ biến thể cho trường hợp có ngày đang treo, và trạng thái 'expired' cho dòng Sổ nợ. Ngày ân hạn vẫn xoá được ngày đã hết hạn — đó là van cho trường hợp mất điện thoại thật
- (event) Sửa lỗi mã hoá: lần đặt status:final bằng PowerShell đã đọc tệp theo ANSI rồi ghi lại UTF-8 làm hỏng toàn bộ ký tự tiếng Việt và gạch ngang dài trong DESIGN.md/EXPERIENCE.md. Đã phục hồi bằng cp1252->utf-8 và kiểm lại
- (event) spines finalized
